Current version 0.3-Beta

Android application for sync birthdays from social networks (facebook.com, vk.com), google contacts and show them in one list.
Supports android 2.2 or higher. Authorization with outer systems made via oath2 with native dialog (app not store passwords).

Birthday app depends on modules sharing on github:
https://github.com/thest1/LazyList.git (lazy image loading)
https://github.com/facebook/facebook-android-sdk.git (3 classes under com.facebook.android package - no need to take all facebook sdk)
